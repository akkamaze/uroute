# Prepare separately after staging login and database checks pass.
# Never commit real credentials or reuse staging credentials.
DATABASE_URL=postgresql://uroute_prod_app:CHANGE_ME@PROD_POSTGRES_ADDRESS:5432/uroute_prod
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=https://trip.synqloud.com
TRUSTED_ORIGINS=https://trip.synqloud.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Only set exact reverse-proxy peer IPs after verifying the tunnel topology.
TRUSTED_PROXY_IPS=
# Select after staging succeeds and the production tunnel route is reviewed.
BIND_ADDRESS=127.0.0.1
WEB_PORT=CHOOSE_UNUSED_PRODUCTION_PORT
